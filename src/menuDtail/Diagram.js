import React from 'react';
import DiagramChart from './DiagramChart';
import { Button } from '@material-ui/core';

const Diagram=()=>{

    
    
    return(
        <>
                {/* 여기다가 map돌리면됨 일부러 파일 분류했음 -엄준식 */}
                {/* <Button style={{width:'100%',textAlign:'unset'}}> */}
                    <DiagramChart/>
                {/* </Button> */}
        </>
    )
}
export default Diagram;
